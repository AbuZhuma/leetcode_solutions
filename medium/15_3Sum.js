var threeSum = function (nums) {
      const sorted = nums.sort()
      for (let i = 0; i < sorted.length; i++) {
            let r = null
            let l = null
            let sum = null
            let finish = false
            while(!finish){
                  if(!r){
                        r = i+1 ? i+1 : i-1
                  }
            }
      }
};